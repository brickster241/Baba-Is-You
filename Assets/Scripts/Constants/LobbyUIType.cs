using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enums {
    /*
        Enum for different types of UI INSIDE Lobby Scene.
    */
    public enum LobbyUIType {
        NONE = 0,
        LEVEL_SELECT = 1,
        MAIN = 2,
        INSTRUCTION = 3
    }

}
